name: retroactive-development
sk_number: SK-419
version: "1.0.0"
load_order: 20
priority: RECOMMENDED
status: APPROVED
description: |
  Engine-level fix propagation for the XIIGen engine. When a bug is found in generated
  code, the fix goes in the ENGINE (AF station, DNA guard, skill block, factory contract)
  — never in the generated output file. After fixing, re-validate ALL flows that use
  the same task type.
triggers:
  - "retroactive"
  - "fix in engine"
  - "generated code bug"
  - "fix propagation"
  - "re-validate flows"
  - "adapt test to accept"
  - "engine fix"
phase_created: 9

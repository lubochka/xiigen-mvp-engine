/**
 * FT-F1 — Canva Site Generator Adapter local spec stub
 *
 * NOTE: Tests run from server/test/flow34/f1-canva-site-adapter.spec.ts
 * using server's ts-jest setup (adapter has no local node_modules yet).
 *
 * This stub documents the test surface for when FT-F1 is a standalone
 * installed package with its own Jest/ts-jest setup.
 *
 * Full coverage: 26 tests (F1-R-1..10, F1-W-1..8, F1-E-1..4, F1-P-1..4)
 */

// Placeholder — see server/test/flow34/f1-canva-site-adapter.spec.ts
describe('FT-F1 stub', () => {
  it('tests run from server/test/flow34/f1-canva-site-adapter.spec.ts', () => {
    expect(true).toBe(true);
  });
});

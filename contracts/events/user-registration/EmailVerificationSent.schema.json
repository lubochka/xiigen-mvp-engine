{
  "schemaVersion": "1.0",
  "eventType": "xiigen.user-registration.email-verification-sent.v1",
  "taskTypeId": "T48",
  "flowId": "FLOW-01",
  "description": "Emitted by T48 EmailVerificationWaitState when a verification email is dispatched (initial or resend).",
  "cloudEventsEnvelope": {
    "type": "xiigen.user-registration.email-verification-sent.v1",
    "source": "xiigen/flows/user-registration-onboarding/t48",
    "specversion": "1.0"
  },
  "payload": {
    "userId": { "type": "string", "description": "Opaque user identifier — no PII (CF-8)" },
    "tokenId": { "type": "string", "description": "Verification token reference (not the token value itself)" },
    "expiresAt": { "type": "string", "format": "date-time", "description": "Token expiry (24h from issuance)" },
    "isResend": { "type": "boolean", "description": "True if this is a resend of an existing verification" },
    "timestamp": { "type": "string", "format": "date-time" }
  },
  "piiGuard": "Token value never included — only tokenId reference. No email address in payload (CF-8).",
  "requiredByFlows": ["FLOW-01"]
}

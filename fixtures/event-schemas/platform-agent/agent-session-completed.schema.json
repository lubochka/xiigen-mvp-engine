{
  "title": "platform-agent-AgentSessionCompleted-event-schema",
  "type": "object",
  "properties": {
    "id": { "type": "string" },
    "timestamp": { "type": "string", "format": "date-time" },
    "entityId": { "type": "string" },
    "source": { "type": "string" },
    "eventType": {
      "type": "string",
      "enum": ["platform-agent.AgentSessionCompleted"]
    },
    "data": {
      "type": "object",
      "properties": {
        "sessionId": { "type": "string" },
        "tenantId": { "type": "string" },
        "userIntent": { "type": "string" },
        "completedAt": { "type": "string", "format": "date-time" },
        "actionsProposed": { "type": "array" },
        "contributionsRecorded": { "type": "array" },
        "superJudgeVerdict": {
          "type": "string",
          "enum": ["OVERRIDE_PASS", "OVERRIDE_BLOCK", "DEFER_TO_AF9"]
        },
        "grade": { "type": "number" }
      },
      "required": ["sessionId", "tenantId", "completedAt"]
    },
    "metadata": { "type": "object" }
  },
  "required": ["id", "timestamp", "entityId", "eventType"]
}

{
  "title": "platform-agent-ConsentRequested-event-schema",
  "type": "object",
  "properties": {
    "id": { "type": "string" },
    "timestamp": { "type": "string", "format": "date-time" },
    "entityId": { "type": "string" },
    "source": { "type": "string" },
    "eventType": {
      "type": "string",
      "enum": ["platform-agent.ConsentRequested"]
    },
    "data": {
      "type": "object",
      "properties": {
        "consentId": { "type": "string" },
        "sessionId": { "type": "string" },
        "contributionId": { "type": "string" },
        "tenantId": { "type": "string" },
        "previewSummary": { "type": "string" },
        "expiresAt": { "type": "string", "format": "date-time" }
      },
      "required": ["consentId", "sessionId", "contributionId", "tenantId"]
    },
    "metadata": { "type": "object" }
  },
  "required": ["id", "timestamp", "entityId", "eventType"]
}

{
  "eventType": "saas.tenant.quota.materialized",
  "flowId": "FLOW-15",
  "version": "1.0.0",
  "properties": {
    "tenantId": { "type": "string", "required": true },
    "tier": { "type": "string", "enum": ["FREE", "STARTER", "PROFESSIONAL", "ENTERPRISE"], "required": true },
    "keyCount": { "type": "integer", "minimum": 1, "required": true },
    "materializedAt": { "type": "string", "format": "date-time", "required": true }
  }
}

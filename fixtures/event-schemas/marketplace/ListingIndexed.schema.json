{
  "schemaVersion": "1.0",
  "eventType": "xiigen.marketplace.listing-indexed.v1",
  "taskTypeId": "T84",
  "flowId": "FLOW-08",
  "description": "Emitted by T84 CatalogIndexer after version-keyed idempotent indexing. idempotency key = hash(listingId + version). Cross-flow dependency on F227 ISearchIndexService from FLOW-07.",
  "cloudEventsEnvelope": {
    "type": "xiigen.marketplace.listing-indexed.v1",
    "source": "xiigen/flows/marketplace/t84",
    "specversion": "1.0"
  },
  "payload": {
    "listingId": {
      "type": "string",
      "description": "Opaque listing identifier"
    },
    "version": {
      "type": "integer",
      "description": "Listing version used in version-keyed idempotency"
    },
    "tenantId": {
      "type": "string",
      "description": "Tenant identifier from AsyncLocalStorage"
    },
    "indexedAt": {
      "type": "string",
      "format": "date-time"
    }
  },
  "piiGuard": "No seller or buyer PII. listingId and tenantId are opaque references.",
  "arbiterRef": "arb-flow08-version-keyed-idempotency: idempotencyKey = hash(listingId + version).",
  "requiredByFlows": ["FLOW-08"],
  "seededAt": "2026-04-13T00:00:00Z"
}

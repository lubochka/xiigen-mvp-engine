{
  "eventType": "query.failed",
  "flowId": "FLOW-13",
  "version": "1.0.0",
  "properties": {
    "queryId": { "type": "string", "required": true },
    "tenantId": { "type": "string", "required": true },
    "reason": { "type": "string", "enum": ["quota_exceeded", "rls_denied", "masking_error"], "required": true },
    "failedAt": { "type": "string", "format": "date-time", "required": true }
  }
}

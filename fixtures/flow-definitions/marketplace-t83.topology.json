{
  "flowId": "FLOW-08",
  "taskTypeId": "T83",
  "name": "flow-08-listing-publisher",
  "description": "T83 ListingPublisher — ROUTING archetype. Audit outbox write is ABSOLUTELY FIRST before moderation, price check, and everything else (DNA-8 strictest form).",
  "archetype": "ROUTING",
  "seededAt": "2026-04-12T00:00:00Z",
  "nodes": [
    {
      "id": "n1",
      "type": "validate",
      "description": "Iron rule validation + DNA compliance check: audit-first-then-all, DNA-8 strictest form, store-before-any-downstream"
    },
    {
      "id": "n2",
      "type": "rag-retrieve",
      "description": "Retrieve ROUTING arch pattern for T83: audit-first outbox, DNA-8 strictest, listing publication routing",
      "params": {
        "archetype": "ROUTING",
        "taskTypeRef": "T83",
        "topK": 5,
        "patternIds": [
          "arch--store-before-enqueue",
          "arch--audit-outbox-first",
          "arch--dna8-strictest-form"
        ]
      }
    },
    {
      "id": "n3",
      "type": "decompose",
      "description": "Break T83 into: storeDocument(audit outbox) FIRST → route to moderation | price check | other downstream actions"
    },
    {
      "id": "n4",
      "type": "ai-generate",
      "description": "Generate listing-publisher.service.ts — audit outbox write is ABSOLUTELY FIRST before moderation, price check, and everything else (DNA-8 strictest form)."
    },
    {
      "id": "n5",
      "type": "validate",
      "description": "Named check validation: audit_first_then_all, dna8_strictest_form, no_downstream_before_audit"
    },
    {
      "id": "n6",
      "type": "score",
      "description": "Score generated code 0-100 against T83 iron rules: audit outbox first, DNA-8, routing correctness"
    },
    {
      "id": "n7",
      "type": "route",
      "description": "Route: score >= 85 → n8 promote, score < 85 → n3 iterate"
    },
    {
      "id": "n8",
      "type": "feedback",
      "description": "DPO triple: chosen=audit-first-then-all, rejected=moderation-before-audit. curriculumTier=1."
    }
  ],
  "edges": [
    { "from": "n1", "to": "n2" },
    { "from": "n2", "to": "n3" },
    { "from": "n3", "to": "n4" },
    { "from": "n4", "to": "n5" },
    { "from": "n5", "to": "n6" },
    { "from": "n6", "to": "n7" },
    { "from": "n7", "to": "n8", "condition": "score >= 0.85" },
    { "from": "n7", "to": "n3", "condition": "score < 0.85 AND cycles_remaining > 0" }
  ],
  "scoreThreshold": 0.85,
  "maxCycles": 3,
  "entry": "ListingPublishRequested command",
  "primaryFabric": "DATABASE"
}
